Fonts — `DM Sans` (body) + `DM Mono` (headings, nav, labels) via Google Fonts
Color scheme — Off-white background (`#f7f6f3`), dark text, single blue accent (`#2d5be3`); all via CSS variables
Layout — Flexbox for nav, skills grid, and contact form; centered `max-width: 720px` container
Selectors — Element, ID, class, and pseudo-class (`:hover`, `:focus`, `:last-of-type`)
Hover effects — Nav links change color, skill cards lift with a blue border, submit button fades slightly
Responsive — `@media (max-width: 480px)` tightens spacing, font sizes, and card widths for mobile

Placed `style.css` in the same directory as `index.html`. 
Linked style.css with index.html with : <link rel="stylesheet" href="style.css">
